{
  "name": "slooze-app",
  "version": "1.0.0",
  "description": "Slooze - Role-Based Food Ordering Platform",
  "scripts": {
  "setup:backend": "cd backend && npm install && npx prisma generate && npx prisma migrate dev --name init && npx ts-node prisma/seed.ts",
  "setup:frontend": "cd frontend && npm install",
  "setup": "npm run setup:backend && npm run setup:frontend",
  "dev:backend": "cd backend && npm run start:dev",
  "dev:frontend": "cd frontend && npm run dev",

  "build": "npm run build --workspace=slooze-backend",
  "start": "npm run start:prod --workspace=slooze-backend"
},
  "workspaces": [
    "backend",
    "frontend"
  ]
}
